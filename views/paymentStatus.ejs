<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Status</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="payment-status-container">
        <% if (status==='SUCCESS' ) { %>
            <h1 class="payment-status-message status-success">Payment Successful</h1>
            <p class="payment-status-details">Thank you for your payment. Your transaction ID is:</p>
            <p class="payment-status-details"><strong>
                    <%= transactionId %>
                </strong></p>
            <p class="payment-status-details">You will receive a confirmation email shortly.</p>
            <% } else { %>
                <h1 class="payment-status-message status-failure">Payment Failed</h1>
                <p class="payment-status-details">Unfortunately, your payment could not be processed. Please try again
                    later or contact support.</p>
                <p class="payment-status-details">Error Message:</p>
                <p class="payment-status-details"><strong>
                        <%= errorMessage || 'Unknown Error' %>
                    </strong></p>
                <% } %>
                    <button class="return-button" onclick="window.location.href='/'">Go Back to Home</button>
    </div>
</body>
</html>